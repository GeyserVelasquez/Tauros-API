<?php

namespace App\Http\Queries;

use Spatie\QueryBuilder\QueryBuilder as SpatieQueryBuilder;

abstract class BaseQuery extends SpatieQueryBuilder
{
    // Lógica compartida o helpers comunes para queries personalizadas si se requieren
}
